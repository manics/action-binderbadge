name: 'binder-badge'
on:
  pull_request:
  # pull_request_target:

jobs:
  badge:
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@v7
      - uses: ./
        with:
          githubToken: ${{ secrets.GITHUB_TOKEN }}
